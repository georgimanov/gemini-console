/**
 * Extension point for merging resources/data (weight, sleep, food, supplements)
 * and resources/templates into a persona's context. Not wired in yet — when it
 * is, the plan is to auto-inject relevant data into the system instruction
 * built in personas.ts, rather than load it via on-demand commands.
 */
